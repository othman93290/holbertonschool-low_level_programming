#include <stdio.h>
/**
 * La fonction main est le point d'entrée du programme.
 * Elle affiche un message simple à l'écran.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void print_alphabet(void);
}
	int letter = 'a';

	for (letter <= 'z')
	{
	putchar (letter);
	letter++;
	}
	{
	print_alphabet();
}
	putchar("\n");
	return (0);
}

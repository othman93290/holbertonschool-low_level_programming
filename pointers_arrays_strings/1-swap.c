#include "main.h"
#include <stdio.h>
/**
 * swap_int - check if c is _abs*
 * @a: The ASCII value of the character to be checked.
 * @b: The ASCII value of the character to be checked.
 * Description: check if c is print_sign*
 */
void swap_int(int *a, int *b)
{
	int c = *a;
	*a = *b;
	*b = c;

}
